// Este arquivo pode ser substituído durante a compilação usando a matriz `fileReplacements`.
// `ng build --prod` substitui` environment.ts` por `environment.prod.ts`.
// A lista de substituições de arquivos pode ser encontrada em `angular.json`.

export const environment = {
  production: false
};

/*
 * Para uma depuração mais fácil no modo de desenvolvimento, você pode importar o seguinte arquivo
 * para ignorar os quadros de pilha de erros relacionados à zona, como `zone.run`,` zoneDelegate.invokeTask`.
 *
 * Essa importação deve ser comentada no modo de produção, pois terá um impacto negativo
 * no desempenho se um erro for gerado.
 */
// import 'zone.js / dist / zone-error'; // Incluído na CLI Angular.
